<%= form_for @artist, url: { action: "update" } do |f| %>
  <%= f.text_field :name %>
  <%= f.text_area :bio, size: "100x50" %>
  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
  <%= f.submit "Update Artist", html: { class: "btn-submit" } %>
<% end %>
