<%= form_for @song, url: { action: "update" } do |f| %>
  <%= f.text_field :name %>
  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
  <%= f.submit "Update Song", html: { class: "btn-submit" } %>
<% end %>
